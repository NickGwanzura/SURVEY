import { RetailerSurveyWizard } from "@/components/survey/RetailerSurveyWizard";

export const metadata = {
  title: "Supplier & Retailer Survey — RAC Technician Registry",
};

export default function RetailerSurveyPage() {
  return <RetailerSurveyWizard />;
}
