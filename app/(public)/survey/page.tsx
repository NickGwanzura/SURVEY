import { SurveyWizard } from "@/components/survey/SurveyWizard";

export const metadata = {
  title: "Survey — RAC Technician Registry",
};

export default function SurveyPage() {
  return <SurveyWizard />;
}
